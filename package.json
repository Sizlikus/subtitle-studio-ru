{
  "name": "subtitle-studio-ru",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "check": "node --check public/app.js && node --check public/subtitles.js && node --experimental-strip-types --check netlify/functions/translate.mts && node --test"
  },
  "dependencies": {
    "@netlify/functions": "6.0.0",
    "openai": "7.16.0"
  }
}
